{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "nlx": false
  },
  "deploy": {
    "numReplicas": 1,
    "restartPolicyType": "OnFailure",
    "restartPolicyMaxRetries": 10,
    "startCommand": "bash -c 'mkdir -p db && bundle exec rails db:prepare && bundle exec rails server -b 0.0.0.0 -p $PORT'"
  }
}
